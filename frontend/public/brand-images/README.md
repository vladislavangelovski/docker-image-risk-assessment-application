# Optional Frontend Images

Drop custom images in this folder to enhance the redesigned UI.

Expected file names:

- `hero-risk-console.jpg` (suggested 1800x1000)
- `compose-blueprint.jpg` (suggested 1400x900)
- `scan-texture.jpg` (suggested 1600x900)

If files are missing, the UI uses CSS fallback gradients automatically.
